# Database Configuration
# Turso Database URL (your remote database)
DATABASE_URL=libsql://your-database.turso.io

# Turso Authentication Token
DATABASE_AUTH_TOKEN=your-auth-token-here

# Optional: Encryption key for local replica encryption at rest
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=your-encryption-key-here

# OAuth Configuration
# GitHub OAuth (if using GitHub authentication)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Optional: Custom replica location (production)
# REPLICA_PATH=/var/data/replica.db

# Optional: Custom sync interval (seconds)
# SYNC_INTERVAL=60
